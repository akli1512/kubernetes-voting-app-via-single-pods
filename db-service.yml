apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: db-service
    app: voting-app
    tier: backend
spec:
  type: ClusterIP
  ports:
    targetPort: 5432
    port: 5432


  selector:
    name: db-pod
    app: voting-app
    tier: backend