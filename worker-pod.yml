apiVersion: v1
kind: Pod
metadata:
  name: worker-pod
  labels:
    name: worker-pod
    app: voting-app
    tier: backend
spec:
  containers:
    -  name: worker-container
       image: example-voting-app_worker
